---
import Layout from "~/layouts/Layout.astro"
import ErrorPage from "~/components/shared/ErrorPage.astro"

const path = Astro.url.pathname
---

<Layout title="Error" path={path}>
    <ErrorPage code="500" title="Internal server error" path={path} />
</Layout>
